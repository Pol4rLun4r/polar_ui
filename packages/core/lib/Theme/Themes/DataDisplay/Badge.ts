import theme from "styled-theming";

export const badgeBackground = theme('mode', {
    light: '#ffffff',
    dark: '#2e2e2e'
})

export const badgeBorder = theme('mode', {
    light: '#ced4da',
    dark: '#424242'
})

export const badgeFontColor = theme('mode', {
    light: '#000000',
    dark: '#ffffff',
})

export const badgeBody = theme('mode', {
    light: '#ffffff',
    dark: '#3b3b3b',
})